# Supabase Configuration
# Get these from https://supabase.com/dashboard/project/_/settings/api
# Copy to .env.local and fill in values before deploy

# ============================================================================
# Frontend Configuration (REQUIRED for app to work)
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# Backend (OPTIONAL - for future server-side features)
# ============================================================================
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# SUPABASE_DB_URL=postgresql://...

# Setup: Copy this file to .env.local, add your Supabase URL + anon key,
# run supabase/schema.sql in Supabase SQL Editor, then npm run dev / deploy.
